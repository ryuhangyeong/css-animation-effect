# 단지 CSS만으로 컨텐츠 길이와 상관없이 동등한 높이를 가지는 반응형 요소

> 이 예제는 충분히 실무에서 쓰일만한 예제이다. 

## 알고 있는 것 & 새롭게 알게 된 것

- 나란히 가로 정렬된 요소가 여러개 있다. 여기에서 가장 긴 요소를 기준으로 나머지 요소의 높이도 동일하게 만들어주고 싶은 경우가 생긴다. 그 경우 `table`과 `table-cell`을 이용하면 쉽게 요구사항을 충족할 수 있다. `table`을 이용하는 경우 `vertical-align`으로 수직 중앙 정렬도 손쉽게 만들 수 있다.
