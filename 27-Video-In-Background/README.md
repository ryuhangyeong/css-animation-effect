# 비디오 배경화면

## 영상을 보기에 앞서 생각해보기

## 알고 있는 것 & 새롭게 알게 된 것

- 요즘은 잘 모르겠고 한때 비디오를 배경으로 하는 사이트가 많았는데 그걸 구현하는 방법을 소개한다.
- 크게 새롭거나 어려운건 없었고 `video` 태그의 속성들만 잘 알아두면 될 듯 하다.
- 그나마 눈여겨 보아야 할 것은 `z-index: -1`이다. `z-index`는 z축에서의 순서를 어떻게 정할지를 명시해주는 것인데 대부분 양수이다. 음수로 한 경우 가장 아래에 배치된다.
