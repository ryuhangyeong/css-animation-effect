# 배경 기울기

## 영상을 보기에 앞서 생각해보기

- `skew` 또는 `polygon`을 통해서 구현할 수 있지 않을까?

## 알고 있는 것 & 새롭게 알게 된 것

- 영상에서는 `skew`을 사용하였다.
- `skew`을 통해서 기울이고 윗쪽 부분(불필요)을 위로 땡겨서 가려주는 방식으로 구현하였다.
- 중요한 것은 `skew` 요소 안까지 영향을 미치므로 안쪽 요소를 별도 영역으로 빼서 `skew`를 반대로 지정해줘야 원래 글자 모양이 나온다.
- 이외에는 별다른 특이사항 없음
